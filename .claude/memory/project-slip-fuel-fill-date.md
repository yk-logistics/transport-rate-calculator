---
name: project-slip-fuel-fill-date
description: สลิปโชว์น้ำมันตามวันเติมจริง (FuelTxn.txn_date) + หลายบิลวันเดียวเป็นบรรทัดย่อยในแถวเดียว — display-only
metadata: 
  node_type: memory
  type: project
  originSessionId: a7be03e9-a1b6-49f9-babb-fcf28b790466
---

DONE+deployed 30มิ.ย. (main df75121): โอสั่งตอนตรวจเงินเดือน — สลิปต้องโชว์น้ำมันใต้ "วันที่เติมจริง" ไม่ใช่ work_date ของ DailyJob ที่คนคีย์ผูกไว้. หลายบิลวันเติมเดียว (B7+B20 หรือผูกผิดวัน) = **อัดในแถวเดียว โชว์เป็นบรรทัดย่อยตัวเล็ก** (ลิตร/บาท ซ้อน) ไม่เพิ่มแถว. ใช้กับสลิปทุกไซต์ทุกคน. โอเลือก visual ผ่าน AskUserQuestion ("ให้วันที่ตรงเลย ไม่ก่อนไม่หลัง").

**ข้อมูลจริง LCB มิ.ย.:** 74/384 บิล `FuelTxn.txn_date = DailyJob.work_date − 1 วัน เป๊ะ` (ที่เหลือ delta=0); `DailyJob.fuel_date`(AC) ว่าง 0/384 → วันเติมจริงเชื่อได้ตัวเดียว = txn_date. **ทั้ง 74 เป็น Case A** (วันเติมจริงมีแถวงานอยู่แล้วเสมอ). DailyJob.fuel_amount == Σ FuelTxn.amount เป๊ะ, ทุก job ผูก 0/1 บิล.

**กลไก (display-only):** generalize `fuel_merge_by_job` → **`fuel_lines_by_job: dict[job_id -> list]`** ใน build_payroll_slip_context. group บิลในตารางตาม txn_date ต่อคน → **anchor = แถวงาน work_date==txn_date** (วันเติมจริง; ไม่งั้น แถวบนสุดของกลุ่ม). anchor ถือ list `[{liter,amount,grade,excluded}]`, job อื่นในกลุ่ม = `[]` (เว้นช่อง ↳). จัดกลุ่มเมื่อ ≥2 บิล หรือ บิลผูกผิดวัน. **ป้าย "ไม่หัก" ตามใบ (exclude_from_driver) ไม่ใช่ host row.** footer Σ + fuel_cost_self + net ไม่เปลี่ยน.

**template:** `_slip_body.html` ทั้ง 2 branch (mixed+ปกติ): `{% set _flines = fuel_lines_by_job|default({}) %}`; `_lines==[]`→`↳`, `_lines` (non-empty)→loop `<div class="fline">` ต่อบิล, else fallback `r.fuel_*`. CSS `.fline` (font เล็ก, dotted separator) ใน payroll_slip.html + payroll_print_all.html. **GOTCHA ซ้ำเดิม: print-all 500** ถ้าลืมเปลี่ยน key ใน `{% with %}` ของ payroll_print_all.html (เปลี่ยน fuel_merge_by_job→fuel_lines_by_job แล้ว).

**verified:** TDD 5 tests (tests/test_slip_fuel_fill_date.py, ลบ test_slip_merge_fuel_same_fill.py เก่า), full suite 256 pass; render นันทสิทธิ์88 จริง (live local DB) reconcile footer 37,733.50 ตรง + **screenshot อ่านออกชัด** (04/06 โชว์ 20/830 B7 + 30/1,081 B20 ซ้อน, 03/06=↳). deploy code-only (ไม่ --with-db).

**UPDATE 1ก.ค. #11 — LCB สะอาดเหมือน BigC + ไม่หัก คนละบรรทัด + debug ZIP:** โอว่า ZIP LCB ยังเก่า. **debug (mantra): ZIP ไม่ได้เก่า** — server render_slip_html (ฟังก์ชัน ZIP ใช้) เป็น layout ใหม่แล้ว (feelines 20, tripv, tag-fuel 0 บน trip driver). ที่ต่าง = **LCB ยังมีป้ายน้ำตาล/↳/เลขเอกสารเทา ที่ BigC เอาออกไปแล้ว** (เพราะ cleanup ทำ `_bigc`-only). โอ: ให้ LCB เหมือน BigC. แก้**ทุกไซต์**: ป้ายน้ำตาล tag-fuel→ข้อความเทา (แต่ **LCB เก็บแถวเติมน้ำมันไว้** เพราะน้ำมันหักจริง วราวุฒิ 15,543 ต้องเห็น ห้ามซ่อน — BigC ข้ามแถวได้เพราะบริษัทออก) ; เอา ↳ ออก ; เอา doc_no เทาออก **แต่คง route_remark** (test_normal_remark_still_shows). **บทเรียน: ZIP export ใช้ payroll_slip.html + build_payroll_slip_context เดียวกับสลิปรายคน → ถ้าสลิปถูก ZIP ถูก ; "ZIP เก่า" มักเป็นไฟล์ download cache หรือ styling site-specific ที่ยังไม่ครบ ไม่ใช่โค้ด stale (พิสูจน์ด้วย render_slip_html บน server เทียบ marker).** ไม่หัก: ย้ายไปบรรทัด fline ที่ 2 (เลิกซ้อนเลข). BigC ลบ "น้ำมันที่ได้" ออกจาก gauge (สับสน).

**UPDATE 1ก.ค. #10 — จัดตารางรอบสุดท้าย (deploy tripv):** โอขอเรียบเป็นคอลัมน์หมด. (1) **ไม่หัก เลขทับ:** fnum/tag-grade/tag-free `flex:none` เล็ก (grade7px/ไม่หัก6.5px) + น้ำมัน฿ 66px → ไม่ทับ. (2) **ทุก fline 8px เท่ากันหมด** `align-items:center` (บิลเดียว=ซ้อน ขนาดเดียว, อยู่กลาง). (3) **ค่าเที่ยว:** `.tripv` 11px (ใหญ่กว่านิด) เรียงคอลัมน์; **พิเศษ/OT/รับตู้ = `.fline.sub` เล็กซ้อนลงมา** (แบบ B7/B20 ไม่ใช่ tag ต่อท้าย) — `feetags`→`feelines` macro. (4) **regroup fuel_lines_by_job:** บิล**ผูกถูกวัน** (job.work_date==txn_date) คงที่แถว job ตัวเอง → **วันเดียวหลายเที่ยว (ปกรณ์) น้ำมันกระจายตามแถวเที่ยว ไม่กระจุกแถวเดียว**; เฉพาะบิล**ผูกผิดวัน**ย้ายไป anchor. reconcile ตรง net ไม่ขยับ. 70 tests pass.

**UPDATE 1ก.ค. #7-9 — slip polish รอบจัดตาราง/SS/boss (deploy แล้ว):**
- **จัดน้ำมัน B7/B20 ตรงคอลัมน์ + แถวสูงเท่า:** บทเรียน — `vertical-align:baseline` กับ cell 2 บรรทัด **ดันแถวสูง** + single-bill กับ merged-bill render คนละโครง → เกรดเด้ง. แก้ = **ทุกแถวใช้โครง `.fline` เดียวกัน** (เดี่ยว=สังเคราะห์ list 1 ตัว, หลายบิล=หลาย fline ย่อด้วย `td:has(.fline+.fline)`) + `.fnum`/`.tag-grade` width คงที่ + flex flex-end → เกรดเรียงคอลัมน์ตรง; `td vertical-align:middle`. col width ต้องไม่เกินความกว้าง column (เคย ค่าเที่ยว 96px ทำ route ยุบ — left col ~424px @900 แต่ ~230px @520 viewport; screenshot ต้องดูที่ความกว้างพิมพ์จริง 900-960px ไม่ใช่ 520).
- **ฐานประกันสังคมต้อง back-calc:** เดิมโชว้ `employee.social_security_base` ตายตัว (9,240) **ผิด** — คน prorate (ลา/เข้ากลางเดือน) ยอดหักจริง≠base×rate. แก้ = **ฐาน = `item.social_security / rate`** → ฐาน × 5% = ยอดหักเป๊ะ เสมอ (วราวุฒิ 358→ฐาน 7,160 ไม่ใช่ 9,240). **กฎ: เลขที่มาที่โชว้บนสลิป ต้อง reconcile กับยอดที่หักจริง ห้ามโชว้ค่า config ตายตัว.**
- **สลิป boss daily table เละ:** มีคอลัมน์เพิ่ม ค่าขนส่งจริง/ราคากลาง/KB ใต้ `table-layout:fixed` → หัวห่อเละ คอลัมน์ล้น. แก้ = class `.daily-boss` → `table-layout:auto` + font 9.5px → คอลัมน์พอดี route เต็ม.

**UPDATE 1ก.ค. #3-6 — slip polish batch (โอ feedback หลายรอบ, ทั้งหมด display-only, deploy ทีละ batch):**
- **#3 fees/SS/deposit/align (fafee47):** ค่าพิเศษ/OT/รับตู้คืนตู้ ต่อแถว **หลังค่าเที่ยว** (tag เล็ก `driver_fees_by_job` จาก DailyJobFee ผ่าน `classify_driver_fee` เดียวกับ engine, reserve fee ไม่หลุด; col ค่าเที่ยว 54→96px). **ซ่อนบรรทัดน้ำมันในสรุปเงินเมื่อ fuel_cost_self==0** (รายเที่ยว): money ribbon "−ค่าน้ำมัน" + "รายได้หลังหักน้ำมัน"(ซ้ำ) + "(ไม่รวมน้ำมัน)" หาย. **ประกันสังคม "(ฐาน X × 5%)"** (employee.social_security_base, ระบุชัด≠รายได้). **"เงินประกัน"→"เงินประกันตน"** ทุก variant (คนขับสับสน). **จัดตาราง: td vertical-align baseline; .fline สีเข้ม(--wb-ink)+flex flex-end; .fnum min-width34+.tag-grade min-width24 → B7/B20 เรียงแถวตรงไม่ขยับ**.
- **#4 เงินเบิก (petty) label (merge fix/slip-petty-label-full):** `_mk_line` ซ่อนชื่อหมวดนำ (กัน "เงินเบิก · เงินเบิก" ซ้ำ) → label=memo เต็ม ไม่ตัด …; `.lr .amt flex-shrink:0 nowrap` + `.lr.sub span overflow-wrap:anywhere`.
- **GOTCHA fee tags:** ครั้งแรกใส่ใน c-route → route ellipsis ตัด tag หาย; ต้องย้ายไป c-trip (ค่าเที่ยว) + .c-trip white-space:normal. **trip_count bug:** วันลาที่บันทึก origin='ลา' (status "ลา / ไม่พร้อม", ไม่มี leave_status) นับเป็นเที่ยว → กันด้วย _IDLE_STATUS set. **identity strip:** เปลี่ยนเป็น "เที่ยว" แล้วโอขอกลับ "วันทำงาน X · ลา Y" (ตัดขาด) เพราะเที่ยวอยู่หัวตารางแล้ว (ซ้ำ).

**UPDATE 1ก.ค. #2 — หัวสลิปนับเที่ยวแทนวัน (commit main 4ac862d):** "รายการวิ่งงาน 36 วัน" = `daily_jobs|length` (จำนวนบรรทัด รวมรถจอด/ลา/เติมน้ำมัน) โอว่าแปลก → เปลี่ยนเป็น **`trip_count` "X เที่ยว"** = นับเฉพาะ `_has_work` (route/ค่าเที่ยว/รายได้) + `leave_status!='leave'`; ตัด รถจอด/ลา/วันเติมน้ำมันล้วน; วันเดียวหลายเที่ยว=นับแยก. หัวตาราง + identity strip โชว์ "X เที่ยว" อย่างเดียว (ตัด วันทำงาน/ลา/ขาด). นันทสิทธิ์ 36→28. ส่ง 3 path (ctx.update/print-all with/zip). deploy marker trip_count verified.

**UPDATE 1ก.ค. (โอ feedback หลังเห็นสลิปจริง):** (1) **เอาลูกศร ↳ ออกจากช่องน้ำมัน** → เว้นช่องว่าง (โองง โดยเฉพาะตกในวันรถจอด); คง ↳ ในคอลัมน์วันที่ (ยุบวันซ้ำ) ไว้. แก้ `_slip_body.html` 2 branch (`<td class="num"></td>` ว่าง). (2) **หน้าสรุป+โอนเงิน พอดี 1 หน้าตัวใหญ่** → `_fitEl(.block)` widthOnly true→**false** (ย่อทั้งสูง+กว้างให้พอ 1 หน้า, ไม่ไหลข้ามหน้า); ตัวใหญ่เป็นค่าเริ่ม คนเยอะค่อย zoom ลง. verified render AYU#18 33คน แต่ละอันพอ 1 หน้า (no-print forms ซ่อนตอนพิมพ์ทำให้แถวสั้นพอ). commit main 82b4dc5, deploy code-only verified (block_fit_false:2 บน server). display-only.

related: [[project-slip-merge-fuel-same-fill]] (รุ่นก่อน B7+B20), [[feedback-slip-fuel-must-reconcile]], [[project-fuel-b7b20-grade]], [[project-lcb-bigc-jun-payroll-review]], [[reference-deploy-mvp-selfverify]], [[project-slip-one-page-per-driver]]
