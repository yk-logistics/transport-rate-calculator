# Fuel B7/B20 Grade + Slip Date-Grouping Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** เก็บเกรดน้ำมัน B7/B20 ต่อบิล (มุมบริหารแยกได้ + ป้ายบนสลิป) และทำให้สลิปไม่โชว์วันที่ซ้ำเมื่อมีหลายบิล/หลายงานในวันเดียว — ทั้งหมดไม่แตะตัวเลขเงิน/payroll.

**Architecture:** เพิ่มฟิลด์ `FuelTxn.fuel_grade` (additive schema v31). Helper เดียว `services/fuel_grade.py` เดาเกรดจากราคา/ลิตร (relative-per-group เป็นหลัก, absolute เป็น fallback) ใช้ร่วมกันทั้ง backfill tool, import, และหน้ากรอก. สลิป (`_slip_body.html`) ยุบการแสดงวันที่ซ้ำด้วย Jinja `loop.previtem` และโชว์ป้ายเกรดต่อบิลจาก mapping `fuel_grade_by_job` ที่ context builder ส่งให้.

**Tech Stack:** FastAPI + SQLModel, SQLite (dev) / PostgreSQL (prod), Jinja2 + HTMX + Tailwind (CDN), pytest.

## Global Constraints

- **ห้ามแตะตัวเลขเงิน**: `fuel_grade` ต้องไม่เข้าไปในสูตร payroll/หักน้ำมันใดๆ. หลังทุก task ที่แตะ DB: net ของ payrun draft + `sum(liter)`/`sum(amount)` ต่อรอบต้องเท่าเดิม.
- **Additive migration เท่านั้น**: เพิ่มผ่าน `_ensure_column` ใน `_apply_additive_migrations`; อัปเดต `SCHEMA_VERSION` พร้อมกัน. ห้าม drop/alter ที่ทำลายข้อมูล.
- **เกรดมีแค่ 3 ค่า**: `"B7"` | `"B20"` | `""` (ไม่ระบุ). ไม่เพิ่มเกรดอื่น (YAGNI).
- **เส้นแบ่งราคา absolute เป็น fallback เท่านั้น** (ราคาน้ำมันผันผวน). หลักคือ relative-per-group (ถูกกว่าในกลุ่ม=B20). ค่าคงที่: `B20_MAX_HINT = 38.0`, `GRADE_GAP_MIN = 3.0`.
- **Backfill ต้อง dry-run ก่อน**: `--dry-run` เป็น default; เขียนจริงต่อเมื่อ `--commit`. เซฟเฉพาะ `fuel_grade` ของแถวที่ยัง `""`.
- รันคำสั่งจาก `ProjectYK_System/app/` ด้วย venv: `.venv/Scripts/python.exe`, `.venv/Scripts/pytest`.
- Commit ทุก task; อยู่บน branch `feat/fuel-b7b20-grade` (มีแล้ว) ไม่ใช่ main.

---

### Task 1: Helper เดาเกรด `services/fuel_grade.py`

**Files:**
- Create: `ProjectYK_System/app/services/fuel_grade.py`
- Test: `ProjectYK_System/app/tests/test_fuel_grade.py`

**Interfaces:**
- Consumes: (none)
- Produces:
  - `guess_grade_from_price(price_per_liter: float) -> str` — คืน `"B7"`|`"B20"`|`""`. `<=0` → `""`; `<= B20_MAX_HINT` → `"B20"`; ไม่งั้น `"B7"`.
  - `assign_grades_for_group(prices: list[float]) -> list[str]` — รับราคา/ลิตรของบิลในกลุ่มเดียว คืน list เกรดเรียงตามเดิม. ถ้า `max-min >= GRADE_GAP_MIN`: บิลที่ราคา `<= (min+max)/2` → `"B20"`, ที่เหลือ → `"B7"`. ถ้าไม่ถึง gap: ใช้ `guess_grade_from_price` รายตัว. ราคา `<=0` → `""` เสมอ.
  - `B20_MAX_HINT = 38.0`, `GRADE_GAP_MIN = 3.0` (module constants)

- [ ] **Step 1: เขียน test ที่ fail**

```python
# ProjectYK_System/app/tests/test_fuel_grade.py
from services.fuel_grade import (
    guess_grade_from_price,
    assign_grades_for_group,
    B20_MAX_HINT,
    GRADE_GAP_MIN,
)


def test_guess_single_price_cheap_is_b20():
    assert guess_grade_from_price(35.2) == "B20"
    assert guess_grade_from_price(36.0) == "B20"


def test_guess_single_price_pricey_is_b7():
    assert guess_grade_from_price(41.2) == "B7"
    assert guess_grade_from_price(40.0) == "B7"


def test_guess_zero_or_negative_is_blank():
    assert guess_grade_from_price(0) == ""
    assert guess_grade_from_price(-5) == ""


def test_group_clear_gap_splits_cheap_b20_pricey_b7():
    # คู่ B7/B20 ในวันเดียว (gap ~6฿) → ถูก=B20 แพง=B7
    assert assign_grades_for_group([41.2, 35.2]) == ["B7", "B20"]
    assert assign_grades_for_group([35.3, 40.8]) == ["B20", "B7"]


def test_group_same_price_both_same_grade():
    # 2 บิลราคาเท่ากัน (เกรดเดียวกัน ไม่ใช่คู่ B7/B20) → absolute fallback ทั้งคู่
    assert assign_grades_for_group([41.2, 41.2]) == ["B7", "B7"]
    assert assign_grades_for_group([35.2, 35.2]) == ["B20", "B20"]


def test_group_single_row_uses_absolute():
    assert assign_grades_for_group([35.2]) == ["B20"]
    assert assign_grades_for_group([41.2]) == ["B7"]


def test_group_three_rows_two_cheap_one_pricey():
    # 27/05 จริง: 35.2, 41.2, 41.2 → B20, B7, B7
    assert assign_grades_for_group([35.2, 41.2, 41.2]) == ["B20", "B7", "B7"]


def test_group_zero_price_stays_blank():
    out = assign_grades_for_group([0.0, 41.2])
    assert out[0] == ""
    assert out[1] == "B7"


def test_constants_present():
    assert B20_MAX_HINT == 38.0
    assert GRADE_GAP_MIN == 3.0
```

- [ ] **Step 2: รัน test ดูว่า fail**

Run: `cd ProjectYK_System/app && .venv/Scripts/pytest tests/test_fuel_grade.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'services.fuel_grade'`

- [ ] **Step 3: เขียน implementation ขั้นต่ำ**

```python
# ProjectYK_System/app/services/fuel_grade.py
"""เดาเกรดน้ำมัน B7/B20 จากราคา/ลิตร.

หลัก: ในการเติมครั้งเดียวรถคันเดียว B20 ถูกกว่า B7 ชัด (~6฿/L) →
เทียบราคา "ภายในกลุ่มเดียวกัน" (relative) เชื่อถือได้กว่าเลขบาทตายตัว
เพราะราคาน้ำมันผันผวนรายวัน. เลข absolute ใช้เป็น fallback ตอนแยกไม่ได้.
fuel_grade เป็นแค่ป้าย — ไม่เข้าไปในสูตรเงินใดๆ.
"""
from __future__ import annotations

B20_MAX_HINT = 38.0   # ราคา <= นี้ เดาเป็น B20 (fallback หยาบ)
GRADE_GAP_MIN = 3.0   # ส่วนต่างราคาในกลุ่มที่ถือว่า "คนละเกรดชัด"


def guess_grade_from_price(price_per_liter: float) -> str:
    if not price_per_liter or price_per_liter <= 0:
        return ""
    return "B20" if price_per_liter <= B20_MAX_HINT else "B7"


def assign_grades_for_group(prices: list[float]) -> list[str]:
    valid = [p for p in prices if p and p > 0]
    if len(valid) >= 2 and (max(valid) - min(valid)) >= GRADE_GAP_MIN:
        mid = (min(valid) + max(valid)) / 2.0
        out = []
        for p in prices:
            if not p or p <= 0:
                out.append("")
            elif p <= mid:
                out.append("B20")
            else:
                out.append("B7")
        return out
    return [guess_grade_from_price(p) for p in prices]
```

- [ ] **Step 4: รัน test ดูว่า pass**

Run: `cd ProjectYK_System/app && .venv/Scripts/pytest tests/test_fuel_grade.py -v`
Expected: PASS ทั้ง 9 เคส

- [ ] **Step 5: Commit**

```bash
git add ProjectYK_System/app/services/fuel_grade.py ProjectYK_System/app/tests/test_fuel_grade.py
git commit -m "feat(fuel): helper เดาเกรด B7/B20 จากราคา (relative-per-group + absolute fallback)"
```

---

### Task 2: เพิ่มฟิลด์ `FuelTxn.fuel_grade` + migration v31

**Files:**
- Modify: `ProjectYK_System/app/models.py` (class `FuelTxn`, หลัง `exclude_from_driver` ~บรรทัด 326)
- Modify: `ProjectYK_System/app/main.py:91` (`SCHEMA_VERSION`) + `_apply_additive_migrations` (~บรรทัด 452, ต่อท้าย v30 block)
- Test: `ProjectYK_System/app/tests/test_fuel_grade_schema.py`

**Interfaces:**
- Consumes: (none)
- Produces: `FuelTxn.fuel_grade: str` (default `""`), คอลัมน์ DB `fueltxn.fuel_grade TEXT DEFAULT ''`.

- [ ] **Step 1: เขียน test ที่ fail**

```python
# ProjectYK_System/app/tests/test_fuel_grade_schema.py
from sqlmodel import Session
from db_config import engine
from models import FuelTxn
from datetime import date
import sqlalchemy as sa


def test_fueltxn_has_fuel_grade_column():
    insp = sa.inspect(engine)
    cols = {c["name"] for c in insp.get_columns("fueltxn")}
    assert "fuel_grade" in cols


def test_fueltxn_fuel_grade_roundtrip():
    with Session(engine) as s:
        row = FuelTxn(txn_date=date(2026, 6, 1), site_code="LCB",
                      plate_no_raw="ZZ-TEST-GRADE", liter=50, amount=1760,
                      fuel_grade="B20", source="test")
        s.add(row)
        s.commit()
        rid = row.id
    with Session(engine) as s:
        got = s.get(FuelTxn, rid)
        assert got.fuel_grade == "B20"
        s.delete(got)
        s.commit()


def test_fuel_grade_defaults_blank():
    with Session(engine) as s:
        row = FuelTxn(txn_date=date(2026, 6, 1), site_code="LCB",
                      plate_no_raw="ZZ-TEST-DEFAULT", liter=10, amount=350,
                      source="test")
        s.add(row)
        s.commit()
        rid = row.id
    with Session(engine) as s:
        got = s.get(FuelTxn, rid)
        assert got.fuel_grade == ""
        s.delete(got)
        s.commit()
```

- [ ] **Step 2: รัน test ดูว่า fail**

Run: `cd ProjectYK_System/app && .venv/Scripts/pytest tests/test_fuel_grade_schema.py -v`
Expected: FAIL — `test_fueltxn_has_fuel_grade_column` คอลัมน์ไม่มี (และ insert จะ error AttributeError/unknown column)

- [ ] **Step 3a: เพิ่ม field ใน model**

ใน `ProjectYK_System/app/models.py` class `FuelTxn`, หลังบรรทัด `exclude_from_driver: bool = Field(default=False)`:

```python
    # B7 / B20 / "" (ไม่ระบุ) — ป้ายเกรดเท่านั้น ไม่เข้าสูตรเงิน (v31).
    fuel_grade: str = Field(default="")
```

- [ ] **Step 3b: bump SCHEMA_VERSION + migration**

ใน `ProjectYK_System/app/main.py` บรรทัด 91:

```python
SCHEMA_VERSION = 31  # v31: FuelTxn.fuel_grade (B7/B20 ป้ายเกรด — ไม่เข้าสูตรเงิน)
```

ต่อท้าย v29→v30 block ใน `_apply_additive_migrations` (หลังบรรทัด `_ensure_column("dailyjob", "gps_rate", "REAL", default="0")`):

```python
    # v30 → v31: FuelTxn เกรดน้ำมัน B7/B20 (ป้ายเกรดเท่านั้น ไม่กระทบสูตรเงิน).
    _ensure_column("fueltxn", "fuel_grade", "TEXT", default="")
```

- [ ] **Step 4: รัน test ดูว่า pass**

Run: `cd ProjectYK_System/app && .venv/Scripts/pytest tests/test_fuel_grade_schema.py -v`
Expected: PASS ทั้ง 3 เคส (migration รันตอน import db_config/init — ถ้า engine ยังไม่ trigger migration, รัน `.venv/Scripts/python.exe -c "import main"` ก่อน 1 ครั้งเพื่อ apply)

- [ ] **Step 5: Commit**

```bash
git add ProjectYK_System/app/models.py ProjectYK_System/app/main.py ProjectYK_System/app/tests/test_fuel_grade_schema.py
git commit -m "feat(fuel): FuelTxn.fuel_grade field + migration v31 (additive)"
```

---

### Task 3: Backfill tool `tools/backfill_fuel_grade.py`

**Files:**
- Create: `ProjectYK_System/tools/backfill_fuel_grade.py`
- Test: `ProjectYK_System/app/tests/test_backfill_fuel_grade.py`

**Interfaces:**
- Consumes: `services.fuel_grade.assign_grades_for_group`, `models.FuelTxn`.
- Produces:
  - `plan_backfill(session) -> list[tuple[int, str]]` — คืน `[(fueltxn_id, grade)]` เฉพาะแถวที่ `fuel_grade == ""` และคำนวณเกรดได้ (ไม่ใช่ `""`). จัดกลุ่มตาม `(site_code, txn_date, plate_no_raw)`, ใช้ราคา = `amount/liter` (liter>0).
  - `apply_backfill(session, plan) -> int` — เซ็ต `fuel_grade` ตาม plan, คืนจำนวนแถวที่เปลี่ยน. เซฟเฉพาะ field นี้.
  - CLI: `python tools/backfill_fuel_grade.py [--commit] [--site LCB]` (default dry-run พิมพ์สรุป).

- [ ] **Step 1: เขียน test ที่ fail**

```python
# ProjectYK_System/app/tests/test_backfill_fuel_grade.py
import sys
from pathlib import Path
from datetime import date

sys.path.insert(0, str(Path(__file__).resolve().parents[2] / "tools"))

from sqlmodel import Session, delete
from db_config import engine
from models import FuelTxn
from backfill_fuel_grade import plan_backfill, apply_backfill


def _mk(s, plate, d, liter, amount, grade=""):
    r = FuelTxn(txn_date=d, site_code="LCB", plate_no_raw=plate,
                liter=liter, amount=amount, fuel_grade=grade, source="test_bf")
    s.add(r); s.flush()
    return r.id


def test_backfill_splits_same_day_pair():
    with Session(engine) as s:
        d = date(2026, 6, 20)
        cheap = _mk(s, "ZZ-BF-1", d, 50, 1760)   # 35.2 -> B20
        pricey = _mk(s, "ZZ-BF-1", d, 50, 2060)  # 41.2 -> B7
        s.flush()
        plan = plan_backfill(s)
        pmap = dict(plan)
        assert pmap[cheap] == "B20"
        assert pmap[pricey] == "B7"
        s.exec(delete(FuelTxn).where(FuelTxn.source == "test_bf"))
        s.commit()


def test_backfill_skips_already_set():
    with Session(engine) as s:
        d = date(2026, 6, 21)
        already = _mk(s, "ZZ-BF-2", d, 50, 1760, grade="B7")  # ตั้งไว้แล้ว
        s.flush()
        plan = plan_backfill(s)
        assert already not in dict(plan)
        s.exec(delete(FuelTxn).where(FuelTxn.source == "test_bf"))
        s.commit()


def test_apply_sets_only_grade_not_amount():
    with Session(engine) as s:
        d = date(2026, 6, 22)
        rid = _mk(s, "ZZ-BF-3", d, 50, 1760)
        s.flush()
        before_amt = s.get(FuelTxn, rid).amount
        n = apply_backfill(s, [(rid, "B20")])
        s.commit()
        got = s.get(FuelTxn, rid)
        assert n == 1
        assert got.fuel_grade == "B20"
        assert got.amount == before_amt  # เงินไม่ขยับ
        s.exec(delete(FuelTxn).where(FuelTxn.source == "test_bf"))
        s.commit()
```

- [ ] **Step 2: รัน test ดูว่า fail**

Run: `cd ProjectYK_System/app && .venv/Scripts/pytest tests/test_backfill_fuel_grade.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'backfill_fuel_grade'`

- [ ] **Step 3: เขียน implementation**

```python
# ProjectYK_System/tools/backfill_fuel_grade.py
"""Backfill FuelTxn.fuel_grade จากราคา/ลิตร (relative-per-group).

เซฟเฉพาะ fuel_grade ของแถวที่ยังว่าง — ไม่แตะ liter/amount/exclude/อื่น ๆ.
Default = dry-run (พิมพ์สรุป). ต้อง --commit ถึงเขียนจริง.

รันจาก ProjectYK_System/app/:
    .venv/Scripts/python.exe ../tools/backfill_fuel_grade.py            # dry-run
    .venv/Scripts/python.exe ../tools/backfill_fuel_grade.py --commit   # เขียนจริง
"""
from __future__ import annotations

import argparse
import sys
from collections import defaultdict
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "app"))

from sqlmodel import Session, select  # noqa: E402
from db_config import engine  # noqa: E402
from models import FuelTxn  # noqa: E402
from services.fuel_grade import assign_grades_for_group  # noqa: E402


def plan_backfill(session: Session, site: str | None = None) -> list[tuple[int, str]]:
    stmt = select(FuelTxn).where(FuelTxn.fuel_grade == "")
    if site:
        stmt = stmt.where(FuelTxn.site_code == site)
    rows = session.exec(stmt).all()
    groups: dict[tuple, list[FuelTxn]] = defaultdict(list)
    for r in rows:
        groups[(r.site_code, r.txn_date, r.plate_no_raw)].append(r)
    plan: list[tuple[int, str]] = []
    for _, members in groups.items():
        prices = [((m.amount or 0) / m.liter) if (m.liter or 0) > 0 else 0.0 for m in members]
        grades = assign_grades_for_group(prices)
        for m, g in zip(members, grades):
            if g:
                plan.append((m.id, g))
    return plan


def apply_backfill(session: Session, plan: list[tuple[int, str]]) -> int:
    n = 0
    for rid, grade in plan:
        row = session.get(FuelTxn, rid)
        if row is not None and row.fuel_grade == "":
            row.fuel_grade = grade
            n += 1
    return n


def main() -> None:
    ap = argparse.ArgumentParser()
    ap.add_argument("--commit", action="store_true", help="เขียนจริง (default dry-run)")
    ap.add_argument("--site", default=None, help="จำกัดไซต์ เช่น LCB")
    args = ap.parse_args()
    with Session(engine) as s:
        plan = plan_backfill(s, site=args.site)
        from collections import Counter
        c = Counter(g for _, g in plan)
        print(f"จะตั้งเกรด: B7={c['B7']}  B20={c['B20']}  (รวม {len(plan)} แถว, แถวที่เดาไม่ได้/มีเกรดแล้ว ไม่นับ)")
        if args.commit:
            n = apply_backfill(s, plan)
            s.commit()
            print(f"COMMITTED: เปลี่ยน {n} แถว")
        else:
            print("DRY-RUN: ยังไม่เขียน — ใส่ --commit เพื่อเขียนจริง")


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: รัน test ดูว่า pass**

Run: `cd ProjectYK_System/app && .venv/Scripts/pytest tests/test_backfill_fuel_grade.py -v`
Expected: PASS ทั้ง 3 เคส

- [ ] **Step 5: รัน dry-run บน DB จริง (ตรวจตัวเลขสมเหตุผล)**

Run: `cd ProjectYK_System/app && .venv/Scripts/python.exe ../tools/backfill_fuel_grade.py --site LCB`
Expected: พิมพ์ประมาณ `B7=~281  B20=~100` (สอดคล้อง histogram รอบ มิ.ย.); ไม่เขียน DB

- [ ] **Step 6: Commit**

```bash
git add ProjectYK_System/tools/backfill_fuel_grade.py ProjectYK_System/app/tests/test_backfill_fuel_grade.py
git commit -m "feat(fuel): backfill tool ตั้ง fuel_grade จากราคา (dry-run default, เซฟเฉพาะ grade)"
```

---

### Task 4: ส่ง grade เข้า context สลิป + ป้ายเกรด + ยุบวันที่ซ้ำ

**Files:**
- Modify: `ProjectYK_System/app/services/payroll_slip.py` (`build_payroll_slip_context`, return dict ~บรรทัด 375-398)
- Modify: `ProjectYK_System/app/templates/_slip_body.html` (CSS ป้าย; cell `c-date` + cell น้ำมัน ทั้ง branch mixed บรรทัด 48/57-62 และ branch ปกติ บรรทัด 84/87-91)
- Modify: `ProjectYK_System/app/templates/payroll_print_all.html` (เพิ่ม CSS `.tag-grade` ในบล็อก `<style>` ให้ตรงกับ slip page; print-all include `_slip_body.html` อยู่แล้ว)
- Test: `ProjectYK_System/app/tests/test_slip_fuel_grade.py`

**Interfaces:**
- Consumes: `FuelTxn.fuel_grade`, `fuel_rows` (มีอยู่แล้วใน builder).
- Produces: context key `fuel_grade_by_job: dict[int, str]` (daily_job_id → grade, เฉพาะที่มี grade) ใช้ใน `_slip_body.html`.

- [ ] **Step 1: เขียน test ที่ fail (context builder ใส่ fuel_grade_by_job)**

```python
# ProjectYK_System/app/tests/test_slip_fuel_grade.py
from datetime import date
from sqlmodel import Session, delete
from db_config import engine
from models import Employee, DailyJob, FuelTxn, PayRun, PayRunItem
from services.payroll_slip import build_payroll_slip_context


def _cleanup(s):
    s.exec(delete(FuelTxn).where(FuelTxn.source == "test_slipgrade"))
    s.exec(delete(DailyJob).where(DailyJob.remark == "test_slipgrade"))


def test_context_has_fuel_grade_by_job():
    with Session(engine) as s:
        emp = Employee(full_name="ทดสอบ เกรด", home_site_code="LCB", pay_mode="lcb_trip")
        s.add(emp); s.flush()
        dj = DailyJob(driver_id=emp.id, site_code="LCB", work_date=date(2026, 6, 2),
                      plate_no_raw="ZZ-SG-1", fuel_liter=50, fuel_amount=2060,
                      remark="test_slipgrade")
        s.add(dj); s.flush()
        ft = FuelTxn(driver_id=emp.id, site_code="LCB", txn_date=date(2026, 6, 2),
                     plate_no_raw="ZZ-SG-1", liter=50, amount=2060, daily_job_id=dj.id,
                     fuel_grade="B7", source="test_slipgrade")
        s.add(ft); s.flush()
        pr = PayRun(site_code="LCB", pay_cycle_tag="2026-06",
                    period_start=date(2026, 5, 16), period_end=date(2026, 6, 15))
        item = PayRunItem(pay_mode="lcb_trip")
        ctx = build_payroll_slip_context(s, pr, emp, item)
        assert ctx["fuel_grade_by_job"].get(dj.id) == "B7"
        _cleanup(s)
        s.delete(emp); s.commit()
```

- [ ] **Step 2: รัน test ดูว่า fail**

Run: `cd ProjectYK_System/app && .venv/Scripts/pytest tests/test_slip_fuel_grade.py -v`
Expected: FAIL — `KeyError: 'fuel_grade_by_job'`

- [ ] **Step 3a: เพิ่ม mapping ใน builder**

ใน `ProjectYK_System/app/services/payroll_slip.py`, ก่อน `return {` (หลังบล็อก `excluded_job_ids = ...` ~บรรทัด 292) เพิ่ม:

```python
    fuel_grade_by_job = {
        f.daily_job_id: f.fuel_grade
        for f in fuel_rows
        if f.daily_job_id and f.fuel_grade
    }
```

ใน return dict เพิ่ม key (ใกล้ `"excluded_job_ids": excluded_job_ids,`):

```python
        "fuel_grade_by_job": fuel_grade_by_job,
```

- [ ] **Step 4: รัน test ดูว่า pass**

Run: `cd ProjectYK_System/app && .venv/Scripts/pytest tests/test_slip_fuel_grade.py -v`
Expected: PASS

- [ ] **Step 5: CSS ป้ายเกรด (ทั้ง 2 template)**

ใน `ProjectYK_System/app/templates/_slip_body.html` — เพิ่ม `<style>` เล็กไม่มี (CSS อยู่ที่ payroll_slip.html / payroll_print_all.html). ฉะนั้นเพิ่มคลาสที่ทั้งสองไฟล์:

`payroll_print_all.html` ในบล็อก `<style>` ใกล้ `.tag-co{...}` (บรรทัด ~59):

```css
.tag-grade{display:inline-block;font-size:8px;background:#eef2f7;color:#3a4a68;padding:0 4px;border-radius:2px;border:1px solid #d4dae3;}
```

ทำเหมือนกันใน `payroll_slip.html` (หน้า `<style>` ของไฟล์นั้น — หาคลาส `.tag-co` แล้วเพิ่ม `.tag-grade` ถัดไป).

- [ ] **Step 6: ป้ายเกรดในเซลล์น้ำมัน — branch ปกติ**

ใน `_slip_body.html` branch ปกติ (เซลล์น้ำมัน บรรทัด 87-91) แก้เป็น:

```html
            <td class="num">
              {%- if r.fuel_amount %}{{ '{:,.0f}'.format(r.fuel_amount) }}
                {%- if fuel_grade_by_job.get(r.id) %} <span class="tag-grade">{{ fuel_grade_by_job[r.id] }}</span>{% endif %}
                {%- if r.id in excluded_job_ids %} <span class="tag-free">ไม่หัก</span>{% endif %}
              {%- endif %}
            </td>
```

- [ ] **Step 7: ป้ายเกรดในเซลล์น้ำมัน — branch mixed**

ใน `_slip_body.html` branch mixed (เซลล์น้ำมัน บรรทัด 57-62) แก้เป็น:

```html
            <td class="num">
              {%- if r.fuel_amount %}{{ '{:,.0f}'.format(r.fuel_amount) }}
                {%- if fuel_grade_by_job.get(r.id) %} <span class="tag-grade">{{ fuel_grade_by_job[r.id] }}</span>{% endif %}
                {%- if r.work_date in fuel_deduct_dates %} <span class="tag-cut">หัก</span>
                {%- elif k.kind == 'trip' %} <span class="tag-co">บริษัท</span>{% endif %}
              {%- endif %}
            </td>
```

- [ ] **Step 8: ยุบวันที่ซ้ำ — branch ปกติ**

ใน `_slip_body.html` branch ปกติ เซลล์วันที่ (บรรทัด 84) แก้เป็น (ใช้ `loop.previtem`):

```html
            <td class="c-date">{% if loop.first or loop.previtem.work_date != r.work_date %}{{ r.work_date | dmy }}{% else %}<span class="wb-muted">↳</span>{% endif %}</td>
```

- [ ] **Step 9: ยุบวันที่ซ้ำ — branch mixed**

ใน `_slip_body.html` branch mixed เซลล์วันที่ (บรรทัด 48) แก้เป็น:

```html
            <td class="c-date">{% if loop.first or loop.previtem.work_date != r.work_date %}{{ r.work_date | dmy }}{% else %}<span class="wb-muted">↳</span>{% endif %}</td>
```

- [ ] **Step 10: ตรวจ render จริง (เลือกคนที่มีวันซ้ำ + เกรด)**

Run: `cd ProjectYK_System/app && .venv/Scripts/pytest tests/test_payroll_print_all.py tests/test_slip_control.py -v`
Expected: PASS (ไม่ regression สลิป/print-all). ถ้ามี test render ที่นับแถว — ต้องไม่เปลี่ยน.

- [ ] **Step 11: Commit**

```bash
git add ProjectYK_System/app/services/payroll_slip.py ProjectYK_System/app/templates/_slip_body.html ProjectYK_System/app/templates/payroll_print_all.html ProjectYK_System/app/templates/payroll_slip.html ProjectYK_System/app/tests/test_slip_fuel_grade.py
git commit -m "feat(slip): ป้ายเกรด B7/B20 ต่อบิล + ยุบวันที่ซ้ำ (display only, ไม่แตะเงิน)"
```

---

### Task 5: หน้า /fuel — กรอก/แก้เกรด + import ตั้งเกรด

**Files:**
- Modify: `ProjectYK_System/app/main.py` — `fuel_save` (Form param + assign, บรรทัด 3040-3093); `_fuel_row_json` (บรรทัด 2974-2989); grid editable set (บรรทัด 3116-3118)
- Modify: `ProjectYK_System/app/templates/fuel_form.html` (dropdown เกรด ใกล้ `fill_type` บรรทัด 81-87)
- Modify: `ProjectYK_System/app/templates/fuel_list.html` (คอลัมน์เกรดในตาราง)
- Modify: `ProjectYK_System/tools/import_lcb_may_jun2026_xlsx.py` (ตั้ง fuel_grade ตอนสร้าง FuelTxn ~บรรทัด 299)
- Test: `ProjectYK_System/app/tests/test_fuel_form_grade.py`

**Interfaces:**
- Consumes: `services.fuel_grade.guess_grade_from_price`, `FuelTxn.fuel_grade`.
- Produces: `fuel_save` รับ Form `fuel_grade`; `_fuel_row_json` คืน key `fuel_grade`; grid editable รวม `fuel_grade`.

- [ ] **Step 1: เขียน test ที่ fail (POST /fuel ตั้งเกรดได้)**

```python
# ProjectYK_System/app/tests/test_fuel_form_grade.py
from fastapi.testclient import TestClient
from sqlmodel import Session, delete, select
import main
from db_config import engine
from models import FuelTxn


def test_post_fuel_new_saves_grade():
    client = TestClient(main.app)
    resp = client.post("/fuel/new", data={
        "txn_date": "2026-06-03", "site_code": "LCB", "plate_no_raw": "ZZ-FF-1",
        "liter": "50", "amount": "1760", "fuel_grade": "B20",
    }, follow_redirects=False)
    assert resp.status_code == 303
    with Session(engine) as s:
        row = s.exec(select(FuelTxn).where(FuelTxn.plate_no_raw == "ZZ-FF-1")).first()
        assert row is not None
        assert row.fuel_grade == "B20"
        s.exec(delete(FuelTxn).where(FuelTxn.plate_no_raw == "ZZ-FF-1"))
        s.commit()


def test_fuel_row_json_includes_grade():
    # _fuel_row_json เป็น closure ใน fuel_list — ตรวจผ่าน GET /fuel ว่า rows_json มี key
    client = TestClient(main.app)
    with Session(engine) as s:
        s.add(FuelTxn(txn_date=__import__("datetime").date(2026, 6, 3), site_code="LCB",
                      plate_no_raw="ZZ-FJ-1", liter=50, amount=1760, fuel_grade="B7",
                      source="test_ffjson"))
        s.commit()
    resp = client.get("/fuel?plate=ZZ-FJ-1")
    assert resp.status_code == 200
    assert "fuel_grade" in resp.text
    with Session(engine) as s:
        s.exec(delete(FuelTxn).where(FuelTxn.source == "test_ffjson"))
        s.commit()
```

- [ ] **Step 2: รัน test ดูว่า fail**

Run: `cd ProjectYK_System/app && .venv/Scripts/pytest tests/test_fuel_form_grade.py -v`
Expected: FAIL — `fuel_grade` ไม่ถูกบันทึก (assert row.fuel_grade == "B20" fail) และ `_fuel_row_json` ไม่มี key

- [ ] **Step 3a: `fuel_save` รับ + เซ็ต fuel_grade**

ใน `ProjectYK_System/app/main.py` `fuel_save` signature เพิ่ม param (หลัง `fill_type: str = Form(""),` บรรทัด 3055):

```python
    fuel_grade: str = Form(""),
```

ในตัว body หลัง `row.fill_type = fill_type` (บรรทัด 3091) เพิ่ม:

```python
        row.fuel_grade = fuel_grade
```

- [ ] **Step 3b: `_fuel_row_json` คืน fuel_grade**

ใน `_fuel_row_json` (บรรทัด 2974-2989) เพิ่มหลัง `"source": r.source or "",`:

```python
            "fuel_grade": r.fuel_grade or "",
```

- [ ] **Step 3c: grid editable set รวม fuel_grade**

ใน `fuel_grid_save` editable set (บรรทัด 3116-3118) เพิ่ม `"fuel_grade"`:

```python
    editable = {"txn_date", "site_code", "plate_no_raw", "driver_raw_name",
                "liter", "amount", "price_per_liter", "rate_km_per_l",
                "mile_snapshot", "station", "fuel_grade"}
```

- [ ] **Step 4: รัน test ดูว่า pass**

Run: `cd ProjectYK_System/app && .venv/Scripts/pytest tests/test_fuel_form_grade.py -v`
Expected: PASS ทั้ง 2 เคส

- [ ] **Step 5a: dropdown เกรดใน fuel_form.html**

ใน `ProjectYK_System/app/templates/fuel_form.html` หลังบล็อก `fill_type` (บรรทัด 81-87 จบที่ `</select>` + `</div>`) เพิ่มกล่องใหม่:

```html
    <div>
      <label class="block text-sm mb-1">เกรดน้ำมัน</label>
      <select name="fuel_grade" class="w-full border rounded px-3 py-2">
        <option value="" {% if not row or not row.fuel_grade %}selected{% endif %}>—</option>
        <option value="B7" {% if row and row.fuel_grade=='B7' %}selected{% endif %}>B7</option>
        <option value="B20" {% if row and row.fuel_grade=='B20' %}selected{% endif %}>B20</option>
      </select>
    </div>
```

- [ ] **Step 5b: คอลัมน์เกรดใน fuel_list.html (Tabulator)**

`fuel_list.html` เป็น Tabulator grid (column definitions array `columns:[...]` ~บรรทัด 298) ไม่ใช่ `<th>/<td>`. เพิ่ม column ใหม่ **หลัง** column "ปั๊ม" (`field:"station"` บรรทัด 344-345) ก่อน column "Job":

```js
      { title:"เกรด ✎",          field:"fuel_grade",      editor:"list",
        editorParams:{ values:{ "":"—", "B7":"B7", "B20":"B20" } },
        hozAlign:"center", width:80,
        headerFilter:makeExcelFilter("fuel_grade"), headerFilterFunc:excelFilterFunc },
```

หมายเหตุ: `fuel_grade` เป็น string ไม่ต้องเพิ่มใน `NUM_FIELDS` (บรรทัด 140). grid save ส่ง field นี้อยู่แล้วเพราะ editable set ฝั่ง server เพิ่ม `fuel_grade` ใน Step 3c. (`_fuel_row_json` ใส่ key `fuel_grade` ใน Step 3b → ROWS มีค่าเริ่มต้น.)

- [ ] **Step 6: import ตั้งเกรด**

ใน `ProjectYK_System/tools/import_lcb_may_jun2026_xlsx.py`:
- เพิ่ม import ใกล้บรรทัด 38 (`from models import ...`): `from services.fuel_grade import guess_grade_from_price`
- แก้ constructor FuelTxn (บรรทัด 299-306, ของจริงใช้ `source=IMPORT_SOURCE`, `mile_snapshot=mile`, ไม่มี `fuel_date`) เพิ่ม `fuel_grade=` 1 บรรทัด:

```python
            if fuel_l > 0 or fuel_amt > 0:
                session.add(FuelTxn(
                    site_code="LCB", txn_date=work_date,
                    plate_no_raw=plate, driver_raw_name=driver,
                    liter=fuel_l, amount=fuel_amt,
                    price_per_liter=(fuel_amt / fuel_l) if fuel_l else 0,
                    fuel_grade=guess_grade_from_price((fuel_amt / fuel_l) if fuel_l else 0),
                    mile_snapshot=mile,
                    daily_job_id=dj.id, source=IMPORT_SOURCE,
                ))
                stats["fuel"] += 1
```

- [ ] **Step 7: รัน test ครบ + ตรวจ /fuel โหลดได้**

Run: `cd ProjectYK_System/app && .venv/Scripts/pytest tests/test_fuel_form_grade.py tests/test_fuel_grade.py tests/test_fuel_grade_schema.py -v`
Expected: PASS ทั้งหมด

- [ ] **Step 8: Commit**

```bash
git add ProjectYK_System/app/main.py ProjectYK_System/app/templates/fuel_form.html ProjectYK_System/app/templates/fuel_list.html ProjectYK_System/tools/import_lcb_may_jun2026_xlsx.py ProjectYK_System/app/tests/test_fuel_form_grade.py
git commit -m "feat(fuel): กรอก/แก้เกรด B7/B20 ที่หน้า /fuel + import ตั้งเกรดอัตโนมัติ"
```

---

### Task 6: Preflight ตรวจเงินไม่ขยับ + รัน backfill จริง

**Files:**
- (ไม่สร้างไฟล์ — รัน inspect + backfill)

**Interfaces:**
- Consumes: ทุก task ก่อนหน้า.

- [ ] **Step 1: snapshot net + fuel sum ก่อน backfill**

Run (จาก `ProjectYK_System/app`):
```
.venv/Scripts/python.exe -c "import sqlite3;c=sqlite3.connect('app.db');print('net',list(c.execute('SELECT pay_run_id,SUM(net_pay) FROM payrunitem GROUP BY pay_run_id')));print('fuel',list(c.execute(\"SELECT source,ROUND(SUM(amount),2),ROUND(SUM(liter),2) FROM fueltxn GROUP BY source\")))"
```
บันทึกผลไว้เทียบ.

- [ ] **Step 2: รัน backfill จริง**

Run: `.venv/Scripts/python.exe ../tools/backfill_fuel_grade.py --commit`
Expected: `COMMITTED: เปลี่ยน ~XXX แถว`

- [ ] **Step 3: ยืนยัน net + fuel sum เท่าเดิม**

รันคำสั่ง Step 1 ซ้ำ → ตัวเลข net ทุก pay_run + fuel sum ทุก source **ต้องเท่าเดิมเป๊ะ** (fuel_grade ไม่อยู่ในสูตร). ถ้าเปลี่ยน = บั๊ก ต้องหยุดสอบสวน.

- [ ] **Step 4: ตรวจการกระจายเกรด**

Run:
```
.venv/Scripts/python.exe -c "import sqlite3;c=sqlite3.connect('app.db');print(list(c.execute(\"SELECT fuel_grade,COUNT(*) FROM fueltxn GROUP BY fuel_grade\")))"
```
Expected: เห็น B7/B20/('' สำหรับ liter=0 หรือเดาไม่ได้) จำนวนสมเหตุผล

- [ ] **Step 5: รัน test suite ที่เกี่ยวข้องทั้งหมด**

Run: `.venv/Scripts/pytest tests/test_fuel_grade.py tests/test_fuel_grade_schema.py tests/test_backfill_fuel_grade.py tests/test_slip_fuel_grade.py tests/test_fuel_form_grade.py tests/test_payroll_print_all.py -v`
Expected: PASS ทั้งหมด

- [ ] **Step 6: Commit (ถ้ามีไฟล์เปลี่ยน เช่น app.db dev — ไม่ commit app.db ตาม .gitignore)**

ไม่ต้อง commit (Task 6 รัน operation; โค้ดทั้งหมด commit แล้วใน Task 1-5). บันทึกผล preflight ลง memory/รายงานโอ.

---

## Deploy (หลังทุก task ผ่าน — display/data feature, gate ผ่านเพราะไม่แตะเงิน)

- merge `feat/fuel-b7b20-grade` → main
- deploy โค้ดขึ้น server (templates + main.py + services + models) ผ่าน `deploy_mvp_to_server.sh`
- **server DB**: ต้องรัน backfill บน server ด้วย (migration v31 รันเองตอน restart; backfill ต้องรันมือ 1 ครั้ง) — scp `backfill_fuel_grade.py` ไป รัน `--commit` บน server, ตรวจ net เท่าเดิม
- verify: /fuel โหลด + เห็นคอลัมน์เกรด; สลิป 1 คนมีวันซ้ำ → วันที่ไม่ซ้ำ + ป้ายเกรดโชว์
