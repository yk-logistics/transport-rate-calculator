# ส่งไม้ Fable → Opus (12 ก.ค. 2026 — Fable หมดเขตจริง **13 ก.ค. ~14:00 ไทย**; อัปเดตล่าสุด 22:36)

> อ่านคู่: CHANGELOG 3 หัวข้อล่าสุด + memory index + docs/HANDOFF_FABLE_TO_OPUS_2026-07-06.md (กติกาทั่วไปยังใช้)
> สถานะระบบ: server v53 (health 200) · เทสต์ 708 passed · ทุก commit push ครบใน main

## งานค้าง — รอสัญญาณอะไร / ได้แล้วทำตามไหน

| งาน | รอสัญญาณ | เมื่อได้สัญญาณ ทำตาม |
|---|---|---|
| **ปิดรอบ LCB 15 ก.ค.** (งานปฏิทินใหญ่ถัดไป) | ถึงวัน 15-16 ก.ค. + ไฟล์ทีมวางที่ `Salary/2026/7.Jul/LCB/` | `docs/PAYROLL_CYCLE_CLOSE_RUNBOOK.md` ทั้งวงจร — pre-check 12ก.ค.: เดลี่/สดย่อยตามทัน **ยกเว้น 🔴 เดลี่ 21/6 ขาดจริง (สดย่อยมีงาน ≥15 รายการ) — โอไลน์ตามทีมแล้ว 12ก.ค. + todo id 24**; **ขั้นแรกวันปิด: เช็คว่าเดลี่ 21/6 ถูกคีย์แล้ว** (สคริปต์เช็คต่อวันเขียนใหม่ได้ 5 นาที: อ่านแท็บ `Daily 16.06.69 - 15.07.69` เล่ม VOLVO id ใน runbook, นับคอลัมน์ 37 ค่าเที่ยวพขร.ต่อวัน — ดู memory lcb-jul-preclose) |
| **AYU ปิด 25 ก.ค.** | ถึงวัน + export ชีทจริง | pre-check 12ก.ค.: ชีทพี่หวานแท็บ `Jul 26` คีย์ถึง 12/7+ ✅; ปิดรอบตาม runbook + กติกา re-import --wipe-prior (ห้าม wipe ถ้าใครแก้ grid) |
| **โอคัดกล่องบิล 306 ใบ / 3.79M** | โอว่าง (ไม่มี dependency เทคนิค) | โอกดเองที่ 📥 กล่องบิล; ถ้าโอถามวิธี → ปุ่มเข้ารถ/Stock/ทิ้งทั้งชุด อยู่ครบ; ทะเบียนรายบรรทัดพิมพ์เลขท้ายได้ |
| **เบิกรายสัปดาห์ LCB + หักเงินทอน** | โอเคาะกติกา 5 ข้อ | `docs/WEEKLY_ADVANCE_LCB_SPEC.md` — สเปค+ลิสต์เทสต์เขียนไว้แล้ว ทำ TDD ตามได้เลย (เรียก yk-money-task ก่อน) |
| **UX รอบถัดไป 8 ข้อ** | โอเลือกข้อ (หรือหยิบตามลำดับในไฟล์) | `docs/UX_REVIEW_2026-07.md` — เรียงตามคุ้ม/ชิ้นแล้ว ทุกข้อจบใน 1 เซสชัน |
| **ทะเบียนใบวางบิล ↔ AR ชีท Drive** | โอตัดสินว่าจะให้ทะเบียน in-app เป็นตัวจริงเมื่อไหร่ | ดู memory project-invoice-registry — ตอนนี้สองระบบแยกกัน (ทะเบียนใหม่เก็บเฉพาะใบที่สร้างตั้งแต่ 12ก.ค.) |
| **บิลไทร์มาร์ทใบอื่นๆ** | เจอใบบริการไทร์มาร์ทในกล่องบิล | โอสั่งยืน: ลงระบบยางรายเส้นเสมอ (ตำแหน่ง/ยี่ห้อ/เบอร์ยาง) — ดูวิธีที่ทำ T0001-2 ใน memory tire |
| รูปใบเคลมประกัน ~40 ใบ (เม.ย.25 ใน PhoneBillScan) | โอสั่งทำโมดูลอุบัติเหตุ | ชี้ไฟล์จาก candidates ใน PhoneBillScan/ (r25_14 คือแผ่นรวม) |
| ลบ/เก็บ PhoneBillScan (~30GB ใน repo, gitignored) | โอสั่งเท่านั้น | ห้ามลบเอง — โอสั่งเก็บทุกอย่างที่เดียวไว้ย้ายเครื่อง |

## ระวัง (ของใหม่ 12ก.ค. ที่อาจมีคำถามตามมา)

- **v52 ประทับเลขใบกลับเดลี่อัตโนมัติ** — เปลี่ยนกติกาเดิมของ C2 (โอเคาะเอง 12ก.ค.); ถ้าทีมงงว่าทำไมเลขขึ้นเอง → อธิบาย + ปุ่มถอนใบอยู่หน้าทะเบียน
- **แถวเดลี่ที่ทีมคีย์เลขใบเองแบบมีขยะท้าย** (`KTIV..-035\t19/6`) ระบบ parse ทนอยู่แล้ว — อย่าไล่แก้ค่าเองกลางงานอื่น; ถ้ามีเหตุต้องล้างจริง ใช้หน้า `/admin/data-clean` เท่านั้น (สร้าง 4ก.ค. มี audit เก็บค่าเดิม + เทสต์คุม)
- คิว OCR กล่องบิล: ถ้าใบใหม่ค้าง pending นาน = ไม่มีใครเปิดหน้า → สั่ง `schtasks /Run /TN YK_BILL_QUEUE_DRAIN` บน server (task SYSTEM มีอยู่แล้ว) หรือรอโอเปิดหน้า
- server เคยดับเอง ~28 นาที (11ก.ค. สาย) กลับมาเอง — ถ้าเกิดซ้ำให้เช็คไฟ/UPS กับโอ
- backup nightly ปกติ (เช็คล่าสุด 12ก.ค.: yk_hot_20260712_030001.zip ครบ)
- QA สแกนมือถือปิดแล้ว: สุ่มรูปคัดทิ้ง 210 ใบ หลุด ~1% และเป็นคลาสที่ไม่เอาอยู่แล้ว — ไม่ต้องกวาดซ้ำ
- สนามสอบเพิ่มข้อ 7 (เช็คไขว้วันว่าง) + ข้อ 8 (กันอัปซ้ำด้วย hash) ใน AGENT_DRILLS จากเคสจริงวันนี้

## 🎓 เพิ่มค่ำ 12ก.ค.: loop "สอน Opus" — สอบผ่าน 11/11 + เอกสารที่ขยับ

- **สอบเพิ่ม 3 ข้อวันนี้ (AGENT_DRILLS ข้อ 9-11) ผ่านหมด** — ข้อ 9 เลขใบขึ้นเอง v52 (ช่องโหว่ที่เจอ: ไม่เปิด HANDOFF → อุดที่ yk-money-task + AGENT_BOOTSTRAP ข้อ 3.5 แล้ว), ข้อ 10 ย้ายแถวข้ามใบ issued (แผนที่ได้ = runbook ย้ายใบจริง อยู่ในตัวข้อสอบ), ข้อ 11 AYU กริดถูกแก้ก่อน wipe (ยืนยัน fix ข้อ 9 ทำงาน)
- **เอกสารที่แก้คืนนี้:** INVOICE_BUILDER_SPEC (กติกา v52), RECEIVABLES_RUNBOOK (ทะเบียน↔AR แยกกัน), PAYROLL_CYCLE_CLOSE_RUNBOOK §2 ข้อ 1 (ท่าแก้กริดถูกแก้ + **gotcha ใหม่: wipe เปลี่ยน id ทุกแถว → เลขใบ v52/link ที่อ้าง id เดิมหลุด — เช็คก่อน wipe เสมอ**), AGENT_BOOTSTRAP (HANDOFF ในลำดับอ่าน + cloud demo = LEGACY), FABLE_MINDSET (ความคิดอันตราย: เอกสารขัดกันต้องชูขึ้นมา)
- ของจริงค้าง 1 จุด (ไม่เร่ง ไม่กระทบเงิน): เดลี่ id 1103 บน server เลขใบมีขยะท้าย — โอล้างเองได้ที่ /admin/data-clean; **โจทย์สอบทั้งหมดเป็นเรื่องสมมติ ไม่มีคำขอจริงจากทีม และไม่มีใครแก้กริด AYU จริง**

## 🔴 เพิ่มบ่าย 12ก.ค.: audit เงินหักค้างท่อ (ดู reports/PETTY_PENDING_AUDIT_2026-07-12.md)

| lead | รอสัญญาณ | ทำตาม |
|---|---|---|
| LCB พ.ค. 20 คน/93,121.77 pending บนรอบ lock-PDF | โอ/ทีมเปิด PDF พ.ค. เทียบรายคน | ตารางในรายงาน — หักแล้ว→mark settled_offline (รายแถวด้วย id); ไม่ได้หัก→C4 |
| ~~สมัย BIGC 10,000~~ แก้แล้ว: ไม่ใช่ค้างท่อ — tag BIGC\|2026-06 ยังไม่ปิด | ตอนปิด BIGC ต้น ส.ค. | เช็คว่ายอดสมัยถูกหัก 10,000 จริงในรอบนั้น |
| ป้องกันซ้ำ | หลังเคลียร์ 2 ก้อนบน | เพิ่มเช็คใน preflight_payrun ตามท้ายรายงาน (TDD) |
